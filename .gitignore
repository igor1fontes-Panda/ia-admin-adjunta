node_modules/
dist/

# Coverage reports (generated locally by `npm run coverage`)
coverage/

# Freebuff hosting build snapshot (platform-managed, changes on every deploy)
isolate/

# Incremental TS build cache (machine-local)
tschedb*
tsconfig.tsbuildinfo
.env
.env.local
.env.*.local
*.local

# Bot-generated local artifacts (when Supabase is not configured)
data/

# Vendored CLI binaries and migration-state snapshots (contain no secrets but are machine-local)
.tools/
supabase-migration-state.txt

# OS / editor
.DS_Store
*.log

# Python bytecode
__pycache__/
*.pyc
.venv-pip-audit/
